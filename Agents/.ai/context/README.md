# Task Context Notes

Create a short Markdown note here only when a multi-turn task has decisions or constraints that do not yet belong in permanent architecture or product documentation.

When the task finishes, move durable information to `Agents/.agent/decisions.md`, `Agents/docs/`, or `Agents/planning/`, then remove the temporary note.
